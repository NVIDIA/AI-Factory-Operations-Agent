# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# You can override the included template(s) by including variable overrides
# SAST customization: https://docs.gitlab.com/ee/user/application_security/sast/#customizing-the-sast-settings
# Secret Detection customization: https://docs.gitlab.com/ee/user/application_security/secret_detection/pipeline/#customization
# Dependency Scanning customization: https://docs.gitlab.com/ee/user/application_security/dependency_scanning/#customizing-the-dependency-scanning-settings
# Container Scanning customization: https://docs.gitlab.com/ee/user/application_security/container_scanning/#customizing-the-container-scanning-settings
# Note that environment variables can be set in several places
# See https://docs.gitlab.com/ee/ci/variables/#cicd-variable-precedence
stages:
  - test
  - publish
  - mirror

sast:
  stage: test

publish-helm-chart:
  stage: publish
  image:
    name: alpine/helm:3.18.4
    entrypoint: [""]
  variables:
    CHART_DIR: helm/mosaic-stack
  script:
    - test -n "$NGC_API_KEY"
    - registry="${NGC_REGISTRY:-nvcr.io/0948643769302270}"
    - base_version="$(helm show chart "$CHART_DIR" | awk '/^version:/ { print $2; exit }')"
    - printf '%s' "$NGC_API_KEY" | helm registry login "${registry%%/*}" --username '$oauthtoken' --password-stdin
    - helm dependency build "$CHART_DIR"
    - helm lint "$CHART_DIR"
    - mkdir -p dist
    - |
      publish() {
        version="$1"
        helm package "$CHART_DIR" --version "$version" --app-version "$CI_COMMIT_SHA" --destination dist
        helm push "dist/mosaic-stack-$version.tgz" "oci://$registry"
      }

      if [ -n "${CI_COMMIT_TAG:-}" ]; then
        release_version="${CI_COMMIT_TAG#v}"
        test "$base_version" = "$release_version"
        publish "$release_version"
      else
        publish "$base_version-sha.$CI_PIPELINE_IID.g$CI_COMMIT_SHORT_SHA"
        if [ "$CI_COMMIT_BRANCH" = "$CI_DEFAULT_BRANCH" ]; then
          major="${base_version%%.*}"
          remainder="${base_version#*.}"
          minor="${remainder%%.*}"
          patch="${base_version##*.}"
          publish "$major.$minor.$((patch + 1))-main.$CI_PIPELINE_IID.g$CI_COMMIT_SHORT_SHA"
        fi
      fi
  rules:
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_BRANCH

github-main-mirror:
  stage: mirror
  image: alpine:3.20
  interruptible: true
  resource_group: github-main-mirror
  variables:
    GIT_DEPTH: "0"
  before_script:
    - apk add --no-cache git openssh-client
    - test -n "$GITHUB_DEPLOY_KEY_B64"
    - install -d -m 700 "$HOME/.ssh"
    - printf '%s' "$GITHUB_DEPLOY_KEY_B64" | base64 -d > "$HOME/.ssh/id_ed25519"
    - chmod 600 "$HOME/.ssh/id_ed25519"
    - ssh-keyscan -H github.com > "$HOME/.ssh/known_hosts"
  script:
    - |
      latest_sha="$(git ls-remote origin "refs/heads/$CI_DEFAULT_BRANCH" | cut -f1)"
      if [ "$latest_sha" != "$CI_COMMIT_SHA" ]; then
        echo "Skipping superseded pipeline for $CI_COMMIT_SHA; main is $latest_sha."
        exit 0
      fi
    - git remote add github git@github.com:NVIDIA/AI-Factory-Operations-Agent.git
    - git push --force github "$CI_COMMIT_SHA:refs/heads/main"
  after_script:
    - rm -rf "$HOME/.ssh"
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
      when: always

include:
  - template: Security/SAST.gitlab-ci.yml

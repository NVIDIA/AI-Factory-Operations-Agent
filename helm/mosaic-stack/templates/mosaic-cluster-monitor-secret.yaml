# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

{{ if and .Values.modules.clusters.enabled .Values.mosaicClusterMonitor.auth.create (not .Values.mosaicClusterMonitor.auth.existingSecret) }}
{{- $existing := lookup "v1" "Secret" .Release.Namespace .Values.mosaicClusterMonitor.auth.name -}}
{{- $existingToken := "" -}}
{{- if and $existing (hasKey $existing.data "token") -}}
{{- $existingToken = (index $existing.data "token" | b64dec) -}}
{{- end -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.mosaicClusterMonitor.auth.name }}
  labels:
    {{- include "mosaic-stack.labels" . | nindent 4 }}
type: Opaque
stringData:
  token: {{ .Values.mosaicClusterMonitor.auth.token | default $existingToken | default (randAlphaNum 48) | quote }}
{{- end }}
